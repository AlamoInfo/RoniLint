[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ronilint"
authors = [{name = "Ronilson Barros", email = "lopesdossantosauta@gmail.com"}]
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []
license = "GPL-2.0-or-later"
[project.scripts]
ronilint = "ronilint.main:main"